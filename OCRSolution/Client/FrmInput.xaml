<Window x:Class="Client.FrmInput"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Client"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="请输入 Baidu Api Key" Height="300" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Button x:Name="btnSumbit" Content="确认" Width="100" />

        <TextBlock Grid.Row="1" Text="ApiID" HorizontalAlignment="Right" VerticalAlignment="Center" />
        <TextBox x:Name="txtApiID" Grid.Row="1" Grid.Column="1" />

        <TextBlock Grid.Row="2" Text="ApiKey" HorizontalAlignment="Right" VerticalAlignment="Center" />
        <TextBox x:Name="txtApiKey" Grid.Row="2" Grid.Column="1" />

        <TextBlock Grid.Row="3" Text="SecretKey" HorizontalAlignment="Right" VerticalAlignment="Center" />
        <TextBox x:Name="txtSecretKey" Grid.Row="3" Grid.Column="1" />

    </Grid>
</Window>
